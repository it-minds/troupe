#!/bin/sh
# A laptop daemon is not a distributed node: no name, no epmd, no port beyond its own.
# `eval` and `start` both work without one; `remote` and `rpc` do not, and are not offered.
export RELEASE_DISTRIBUTION=none
