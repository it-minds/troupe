{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the GUI may ask the shell to do. The HTTP scope is http(s) to anywhere because the plane and its workers are wherever the person's organisation put them; everything else here is a single verb. Nothing grants filesystem, shell or process access: when the local daemon lands those arrive as named commands with their own capability, not by widening this one.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "opener:allow-open-url",
    {
      "identifier": "http:default",
      "allow": [{ "url": "http://*" }, { "url": "https://*" }]
    }
  ]
}
