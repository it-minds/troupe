{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the GUI may ask the shell to do, and nothing more. The HTTP scope is http(s) to anywhere because the plane and its workers are wherever the person's organisation put them; the opener is one verb; the credential store is three commands that take a key and no path. No filesystem, shell or process access: when the local daemon lands, those arrive as named commands with their own capability rather than by widening this one.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    {
      "identifier": "http:default",
      "allow": [
        {
          "url": "http://*"
        },
        {
          "url": "https://*"
        }
      ]
    }
  ]
}
